import React from "react";

function Sermons() {
  return (
    <div
      style={{
        height: "100vh",
        width: "100%",
        background: "orange",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Sermons
    </div>
  );
}

export default Sermons;
